import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kigalirealestate.app',
  appName: 'Kigali Real Estate',
  webDir: 'out'
};

export default config;
